# 升配Redis实例 — 检修操作与回滚参考

**来源文档：**
1. `内网供应链redis实例升配检修方案.docx`（简称"供应链" — 最规范）
2. `内网i6000性能拨测系统redis升配.docx`（简称"i6000"）
3. `外网i国网企业微信redis升配.docx`（简称"企业微信"）

**对应操作类型：** 升配

**生效范围：** 第3节（实例信息）、第6节（实施步骤）、第7节（回滚步骤）的默认文案模板。

---

## 与"创建"操作的对比

| 维度 | 创建 | 升配 |
|------|------|------|
| 3. 实例表格 | 9列 | 5列（名称/ID/当前规格/目标规格/环境） |
| 5.3.2 备份 | 不涉及备份 | **检查备份正常** |
| 5.3.3 验证 | 检查资源集IP充足 | **检查实例运行正常** |
| 6.3.1 小标题 | `创建redis实例` | `{title}`（含项目全称） |
| 6.3 操作步骤 | 3段（登陆→规格→网络→提交） | **3段**（登陆+变更→选规格→等完成） |
| 6.3 实例重复 | 表格（9列） | 表格（5列） |
| 7.1 回滚 | 删除新建实例 | **按照检修回退原实例** |

---

## 一、第3节：实例信息

使用标准表格。3份模板中有2份用了文本行、1份用了表格。**建议统一用表格**（与供应链模板一致）。

**表头：** 实例名称 | 实例ID | 当前规格 | 目标规格 | 环境

**供应链示例：**

| 实例名称 | 实例id | 当前规格 | 目标规格 | 环境 |
|---------|--------|---------|---------|------|
| 供应链运营中心生产REDIS | r-9ur535772a1a4984 | 4G主从版 | 8G主从版 | 内网 |

---

## 二、第6节：实施步骤

### 6.1 备份

**模板原文：**

> 1、{title}检查备份正常

3份文档一致。与创建不同——升配涉及已有实例，需要先确认备份可用。

---

### 6.2 检修前验证

**模板原文：**

> 1、{title}检查实例运行正常

3份文档一致。

---

### 6.3 检修操作

#### 6.3.1 小标题

| 文档 | 6.3.1 小标题 |
|------|-------------|
| 供应链 | `6.3.1 内网供应链redis实例升配` |
| i6000 | `6.3.1 内网i6000性能拨测系统redis升配` |
| 企业微信 | `6.3.1 外网i国网企业微信redis升配` |

**规律：** 使用完整 title，格式为 `6.3.1 {title}`。

---

#### 6.3.1 下重复实例表格

3份文档都在 6.3.1 下再次展示实例信息。建议与第3节保持一致（表格形式）。

---

#### 操作步骤

**供应链模板（最规范，2段）：**
> 使用账号zongbu-shiwenle登陆内网云ASCM平台，产品选择"云数据库Redis"，选择组织"国网物资部"-资源集"供应链运营中心资源集"， 点击进入指定Redis实例-实例信息，点击"变更配置"
>
> 按需选择架构类型和实例规格，点击提交

**企业微信/i6000 还额外加了第三段：**
> 等待任务完成。

**模板提炼（推荐3段式，更完整）：**

> 使用账号{ascm_account}登陆内网云ASCM平台，产品选择"云数据库Redis"，选择组织"{org}"-资源集"{resource_set}"，点击进入指定Redis实例-实例信息，点击"变更配置"
>
> 按需选择架构类型和实例规格，点击提交
>
> 等待任务完成。

**变量说明：**

| 变量 | 来源 | 示例 |
|------|------|------|
| `{ascm_account}` | `risk_assessment.safety_measures.ascm_account` | `zongbu-shiwenle` |
| `{org}` | `environment.organization` | `国网物资部` |
| `{resource_set}` | `environment.resource_set` | `供应链运营中心资源集` |

---

### 6.4 检修后验证

**模板原文（3份文档一致）：**

> 1、验证{title}正常；联系项目组验证业务正常；

与创建/回收相同。

---

## 三、第7节：回滚步骤

### 7.1 回滚操作

**模板原文：**

| 文档 | 回滚措辞 |
|------|---------|
| 供应链 | `按照检修回退原实例` |
| i6000 | `按照检修改回原配置` |
| 企业微信 | `按照检修改回原配置` |

**模板提炼（推荐使用供应链的措辞）：**

> 1、{title}回退
>
> 按照检修回退原实例

---

### 7.2 回滚后验证

**模板原文（与创建/回收一致）：**

> 1、{title}回退验证
>
> 验证回退正常；联系项目组验证业务正常

---

## 四、"升配"操作的关键约定

1. **备份描述：** `{title}检查备份正常`（升配影响已有实例，必须先确认备份可用）
2. **验证描述：** 始终"检查实例运行正常"
3. **实例展示：** 标准表格，5列（名称/ID/当前规格/目标规格/环境）
4. **操作结构：** 3段式（登陆+变更→选规格→等完成）
5. **6.3.1 小标题：** `{title}`（使用完整项目标题）
6. **回退描述：** `按照检修回退原实例`
7. **操作描述使用自然段落，不拆分编号。**
