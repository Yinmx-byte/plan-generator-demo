---
name: rds-maintenance-plan
description: RDS/DRDS 数据库检修方案 Skill。用户提到 RDS、DRDS、创建 RDS、RDS 参数调整、RDS 升配/降配、磁盘扩容、回收 RDS 实例时使用。
owner: cloud-ops
version: 0.2.0
---

# RDS/DRDS 检修 Skill

## 适用场景

- 创建 RDS / DRDS 实例。
- 回收/释放空闲 RDS 实例。
- RDS 实例升配、降配、规格变更。
- RDS 磁盘扩容。
- RDS 参数调整（白名单、参数组等）。

## 文档结构规则（强制）

生成方案必须严格遵循以下七章结构，不得省略任何章节：

1. 一、背景
2. 二、检修类型
3. 三、现场环境
4. 四、实施计划
   - 4.1 检修窗口
   - 4.2 实施人员
5. 五、风险评估
   - 5.1 影响范围
   - 5.2 危险点分析
   - 5.3 安全措施
     - 5.3.1 授权
     - 5.3.2 备份
     - 5.3.3 验证
     - 5.3.4 双人复核
6. 六、实施步骤
   - 6.1 备份
   - 6.2 检修前验证
   - 6.3 检修操作
   - 6.4 检修后验证
7. 七、回滚步骤
   - 7.1 回滚操作
   - 7.2 回滚后验证

每个章节使用 Heading 1，子章节使用 Heading 2，三级使用 Heading 3。

## 章节内容规则

### 一、背景
- 写明涉及的系统名称和本次检修事项概述。
- 写明检修原因（如：项目组确认无业务流量、资源闲置、规格不足等）。

### 二、检修类型
- 使用 checkbox_group 块输出检修类型勾选项，per_line 设为 3。
- 必须包含全部六个选项，仅勾选与本次检修实际匹配的一项，其余留空（□）：
  - 配置变更（checked: true，默认）
  - 组件升级（checked: false）
  - 组件扩缩容（checked: false）
  - 数据库变更（checked: false）
  - 日常维护（原硬件设备）（checked: false）
  - 其他（checked: false）

### 三、现场环境
- 列出检修场景（内网/外网）、实施地点、涉及实例信息。
- 实例信息使用 key_values 块列出：实例ID、实例名称、数据库类型、规格、存储、网络类型、所属组织、资源集。

### 四、实施计划
- 4.1 检修窗口：table 块输出，columns 为 `年份`、`开始时间`、`结束时间`。rows 仅一行数据：
  - `年份`：填入 schedule_year（如"2026年"）。
  - `开始时间`：填入月日+时间（如"06月23日 22:00"）。
  - `结束时间`：填入月日+时间（如"06月23日 23:00"）。
- 4.2 实施人员：table 块输出，columns 为 `角色`、`人员`。rows 必须包含以下 5 行，不可增减：
  - 方案提供人 / 填入 provider 姓名。
  - 检修执行人 / 填入 executor 姓名。
  - 检修复核人 / 填入 reviewer 姓名。
  - 业务系统负责人 / 无则填"不涉及"。
  - 安全责任人 / 填入 security_officer 姓名。

### 五、风险评估
- 5.1 影响范围：写明本次检修影响的具体系统、实例及对业务的影响程度。
- 5.2 危险点分析：必须包含以下四类危险点，每类用编号列出：
  (1) 授权类危险点：授权错误导致操作影响预期外的业务或无法操作目标实例。
  (2) 数据备份类危险点：操作前未进行备份或备份无效导致失败后无法应急恢复。
  (3) 验证类危险点：操作前后未验证实例状态、业务连通性导致异常未被发现。
  (4) 双人复核类危险点：单人操作未复核导致误操作或遗漏。
- 5.3 安全措施
  - 5.3.1 授权：列出 ASCM 平台授权信息和对应账号。
  - 5.3.2 备份：根据操作类型写明备份策略。创建类操作可写"不涉及备份"；回收类操作写明确认实例已关机/无连接，不涉及备份；变更类操作写明备份范围。
  - 5.3.3 验证：写明操作前后需验证的具体项目。
  - 5.3.4 双人复核：写明复核人及复核内容。

### 六、实施步骤
- 6.1 备份：根据操作类型填写具体备份操作或说明不涉及。
- 6.2 检修前验证：写明检修前需确认的状态（实例状态、业务连接、配置参数等）。
- 6.3 检修操作：按操作类型写出具体步骤，必须包含：
  - 登录 ASCM 平台。
  - 选择 RDS/DRDS 数据库产品。
  - 选择组织和资源集。
  - 定位目标实例。
  - 执行具体操作（创建/回收/升配/降配/扩容/参数修改）。
  - 提交前复核。
- 6.4 检修后验证：写明检修后需验证的项目（实例状态、业务连通性、参数生效等）。

### 七、回滚步骤
- 7.1 回滚操作：根据操作类型给出具体回滚方式。
  - 创建类：删除新建实例。
  - 回收类：如实例未彻底释放可恢复，否则写"回收操作不可逆，回滚方式为重新创建实例并恢复数据"。
  - 变更类：按变更前配置恢复。
- 7.2 回滚后验证：回滚后验证业务恢复正常。

## 操作类型细则

### 回收 RDS 实例
- 背景重点：项目组确认无业务连接，实例已关机或可关机。
- 影响范围：对业务无影响（实例已空闲）。
- 备份：回收不涉及备份操作。
- 检修前验证：确认实例状态（已关机或无连接）、确认无业务流量、确认 DNS/业务入口已解绑。
- 检修操作：登录 ASCM → 选择 RDS 产品 → 选择组织资源集 → 定位目标实例 → 执行释放/删除 → 复核确认。
- 检修后验证：确认实例列表中已无该实例（写明"实例已不在控制台"），确认相关业务系统无异常告警。
- 回滚：回收不可逆，若误删需重新创建实例并恢复数据。

### 创建 RDS 实例
- 背景重点：业务需求新建数据库实例。
- 影响范围：对业务无影响（新建操作）。
- 备份：不涉及备份。
- 检修前验证：确认参数（类型、版本、规格、存储、网络、白名单）无误。
- 检修操作：登录 ASCM → 选择 RDS 产品 → 选择组织资源集 → 创建实例 → 填写参数 → 提交。
- 检修后验证：确认实例创建成功、状态为运行中、网络可达。
- 回滚：删除新建实例。

### RDS 升配/降配
- 背景重点：业务需求调整实例规格。
- 影响范围：升配通常无影响，降配需评估资源是否充足。
- 备份：操作前对实例进行备份或确认已有自动备份。
- 检修前验证：确认当前规格、目标规格、实例状态正常。
- 检修操作：登录 ASCM → 选择实例 → 变更规格 → 选择目标规格 → 提交。
- 检修后验证：确认新规格生效、实例状态正常、业务连通性正常。
- 回滚：恢复变更前规格。

### RDS 磁盘扩容
- 背景重点：磁盘使用率接近阈值，需扩容。
- 影响范围：通常对业务无影响（在线扩容）。
- 备份：操作前确认已有备份。
- 检修前验证：确认当前磁盘使用率、目标容量。
- 检修操作：登录 ASCM → 选择实例 → 磁盘扩容 → 填写目标容量 → 提交。
- 检修后验证：确认磁盘容量已变更、实例状态正常。
- 回滚：磁盘扩容通常不可逆，若误操作需评估影响。

### RDS 参数调整
- 背景重点：业务需求调整数据库参数或白名单。
- 影响范围：参数调整可能需重启生效，需评估重启影响。
- 备份：操作前备份当前参数配置。
- 检修前验证：确认当前参数值、目标参数值。
- 检修操作：登录 ASCM → 选择实例 → 参数设置 → 修改参数 → 提交。
- 检修后验证：确认参数已生效。
- 回滚：恢复原参数值。

## 表格要求

以下场景必须使用 table 块输出表格（至少 2 个）：
- 检修窗口表（4.1）：3 列（年份、开始时间、结束时间），1 行数据。
- 实施人员表（4.2）：2 列（角色、人员），5 行固定角色。
- 升配/降配/创建类操作：额外输出实例信息表，列包含实例ID、实例名称、当前规格、目标规格等。
注意：三、现场环境使用 key_values 块，不需要表格。

## RDS 参数表列

```json
[
  {"key": "instance_id", "label": "实例ID"},
  {"key": "instance_name", "label": "实例名称"},
  {"key": "db_type", "label": "数据库类型"},
  {"key": "db_version", "label": "数据库版本"},
  {"key": "current_spec", "label": "当前规格"},
  {"key": "target_spec", "label": "目标规格"},
  {"key": "storage", "label": "存储空间"},
  {"key": "network", "label": "网络类型"},
  {"key": "env", "label": "环境"}
]
```

## 风险与回滚

- 创建 RDS 通常对业务无影响。
- 回收 RDS 需二次确认无业务连接、实例已关机。
- 配置变更（升配/降配/扩容/参数）需检查备份或快照。
- 白名单变更需重点复核访问源和业务连通性。
- 回滚方式与操作类型一一对应。

## 质量评估契约

### common

- 背景
- 检修类型
- 配置变更
- 组件升级
- 组件扩缩容
- 数据库变更
- 日常维护
- 现场环境
- 实施计划
- 检修窗口
- 实施人员
- 风险评估
- 影响范围
- 危险点分析
- 安全措施
- 授权
- 备份
- 验证
- 双人复核
- 实施步骤
- 检修前验证
- 检修操作
- 检修后验证
- 回滚步骤
- 回滚操作
- 回滚后验证
- 实例ID
- 数据库类型

### recycle

- 已关机
- 无业务连接
- 回收
- 实例列表中已无
- 不可逆
- 重新创建

### create

- 创建
- 运行中
- 网络可达
- 删除新建实例

### resize

- 升配
- 降配
- 扩容
- 规格变更
- 备份
- 恢复变更前

### restart

- 参数调整
- 重启
- 恢复原参数值
